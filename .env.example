# Copy this file to .env and fill values before running the app
# Do NOT commit your real .env to git

# Basic app / auth
BASE_URL=http://localhost:3000
SESSION_SECRET=replace_with_a_secure_random_value

# Microsoft / Azure AD (for app login)
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
AZURE_TENANT_ID=

# Email (Gmail) - Option A: App Password (recommended when using account with 2FA)
EMAIL_USER=your.email@gmail.com
EMAIL_PASS=your_16_char_app_password_here

# Email - Option B: OAuth2 (alternative to App Password)
# OAUTH_CLIENT_ID=
# OAUTH_CLIENT_SECRET=
# OAUTH_REFRESH_TOKEN=

# Optional: other envs used by app
#NODE_ENV=development
